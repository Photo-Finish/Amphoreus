# UI archive

Frozen snapshots of Sanctuary UI files so later pictorial work cannot erase them.

See [`docs/UI-ORIGINALS.md`](../UI-ORIGINALS.md) for tags, SHAs, and restore commands.

`pre-pictorial-113d7c2/` is a verbatim checkout of the six UI files at commit
`113d7c2` (`ui-pre-pictorial`).
