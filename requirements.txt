# Project Amphoreus — Python Dependencies

# Core
openai>=1.0.0
python-dotenv>=1.0.0   # loads .env / SENSES_MODE (used by llm_client + senses)

# UI
streamlit>=1.28.0
pandas>=1.5.0          # used by the Map tab (travel-time matrix)
# Optional desktop window (AmphoreusSanctuary.exe): pywebview + pyinstaller
#   pip install pywebview pyinstaller
#   powershell -ExecutionPolicy Bypass -File tools/build_desktop_exe.ps1

# Vector Database (Phase 2 — RAG)
chromadb>=0.4.0

# Embeddings (Phase 2 — RAG)
# sentence-transformers>=2.2.0  # Uncomment for local embeddings

# Senses — hearing (speech-to-text) + video frame extraction (Sanctuary build)
faster-whisper>=1.2.0
av>=13.0.0          # PyAV — video frame extraction (bundled with faster-whisper)
Pillow>=10.0.0      # image handling for eyesight

# Optional: local LLM support
# ollama>=0.1.0

# Development
pytest>=7.0.0
