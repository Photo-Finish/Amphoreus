# Amphoreus OPLoRA training stack (isolated .venv-oplora)
# Install torch FIRST from the cu128 index (see setup_env.ps1), then:
#   pip install -r tools/oplora/requirements.txt

transformers>=4.46.0
peft>=0.13.0
accelerate>=1.0.0
datasets>=3.0.0
trl>=0.12.0
bitsandbytes>=0.45.0
sentencepiece>=0.2.0
protobuf>=4.25.0
safetensors>=0.4.0
pyyaml>=6.0
tqdm>=4.66.0
einops>=0.8.0
